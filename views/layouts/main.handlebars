<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Морской бой</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body data-user-id="{{userId}}" data-nickname="{{nickname}}" data-user-status="{{status}}">
    <header>
        <h1>Морской бой</h1>
         {{#if nickname}}
         <div class="user-info">
             Привет, {{nickname}}!
             <button id="logoutButton" class="button small">Выйти</button>
         </div>
         {{/if}}
    </header>

    <main>
        <div id="messageArea" class="message-area"></div>
        {{{body}}}
    </main>

    <footer>
        <p>&copy; 2025 Морской бой</p>
    </footer>

    <script src="/script.js"></script>
</body>
</html>